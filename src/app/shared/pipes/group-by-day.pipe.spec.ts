import { GroupByDayPipe } from './group-by-day.pipe';

describe('GroupByDayPipe', () => {
  it('create an instance', () => {
    const pipe = new GroupByDayPipe();
    expect(pipe).toBeTruthy();
  });
});
